spring:
  data:
    mongodb:
      uri: mongodb+srv://AbhishekPeiris:abhi2001@cluster0.yiebxsw.mongodb.net/smart_campus_operations_hub

server:
  port: 8080

application:
  security:
    jwt:
      secret-key: smart_campus_operations_hub_123456789
      expiration: 86400000

file:
  upload-dir: uploads/tickets
  max-attachments-per-ticket: 3
  max-file-size: 5242880